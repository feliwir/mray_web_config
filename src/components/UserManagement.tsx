import React from 'react';
import { Typography } from '@mui/material';

const UserManagement: React.FC = () => {
  return (
    <div>
      <Typography variant="h6">User Management Section</Typography>
      <p>Manage your users here.</p>
    </div>
  );
};

export default UserManagement;
